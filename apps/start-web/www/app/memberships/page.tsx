import {Memberships} from '@memba-nx/start-web'
import {membershipsContent} from '@memba-nx/shared'

const MembershipsPage = () => {
  return <Memberships content={membershipsContent} />
}

export default MembershipsPage
