{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "cli": "nx",
  "properties": {
    "command": {
      "type": "string",
      "description": "CDK command to be run, eg context, synth, bootstrap"
    },
    "app": {
      "type": "string",
      "description": "what app to run the cdk against, only needed if multiple apps."
    },
    "stacks": {
      "type": "string",
      "description": ""
    },
    "context": {
      "type": "object",
      "description": "Key-value pairs"
    },
    "trace": {
      "type": "boolean"
    },
    "strict": {
      "type": "boolean"
    },
    "json": {
      "type": "boolean"
    },
    "debug": {
      "type": "boolean"
    },
    "force": {
      "type": "boolean",
      "description": "Always deploy stack even if templates are identical."
    },
    "noColour": {
      "type": "boolean"
    },
    "profile": {
      "type": "string",
      "description": "Name of the AWS Profile to use"
    },
    "outputsFile": {
      "type": "string",
      "description": "Path to create JSON outputs file"
    },
    "outputPath": {
      "type": "string",
      "description": "Emits the synthesized cloud assembly into a directory"
    },
    "all": {
      "type": "boolean",
      "description": "Deploy all CDK stacks defined in cdk application"
    }
  }
}
