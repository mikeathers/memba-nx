{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "OpennextApp",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    },
    "frontEndProjectName": {
      "type": "string",
      "description": "the NextJS project this cdk app associates with",
      "x-prompt": "what is the name of the associated nextjs app? (e.g landing-www)",
      "alias": "p"
    },
    "tags": {
      "type": "string",
      "description": "Add tags to the project (used for linting)",
      "alias": "t"
    },
    "directory": {
      "type": "string",
      "description": "the name of your directory in kebab-case",
      "x-prompt": "What is the name of your app directory? (use kebab-case)",
    }
  },
  "required": ["name", "frontEndProjectName", "directory"]
}
