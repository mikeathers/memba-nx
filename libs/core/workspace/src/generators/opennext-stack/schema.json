{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "OpennextApp",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use? (e.g cdk or www)"
    },
    "tags": {
      "type": "string",
      "description": "Add tags to the project (used for linting)",
      "alias": "t"
    },
    "projectFolderName": {
      "type": "string",
      "description": "the name of your project folder",
      "x-prompt": "What is the name of your app folder?  (e.g id-web)"
    }
  },
  "required": ["name", "projectFolderName"]
}
